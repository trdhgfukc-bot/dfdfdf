# Задание на практическое занятие №6

## Пререквизиты

Выполните пулл ветки ```homework6``` в свой репозиторий с домашними заданиями:

```bash
git pull origin homework6
```

Переключитесь на ветку ```homework6```:

```bash
git checkout homework6
```

Реализуйте функции в соответствии с заданием в файле ```src/advanced_array_function.c```

Выполните пуш ветки в свой удалённый репозиторий. Для того чтобы задание было зачтено, все тесты должны быть пройдены.

При выполнении заданий можно запускать тесты из локального репозитория командами

```bash
make test1
make test2
make test3
```

Для первой, второй и третьей функции соответственно

## Задания

1. Реализовать функцию ```int max_subarray_sum(int* nums, int size)```, которая  принимает целочисленный массив и его размер. И находит непрерывный подмассив (содержащий хотя бы один элемент) в целочисленном массиве, который имеет наибольшую сумму. Если все числа отрицательные, алгоритм все равно должен корректно работать (возвращая наименьшее по модулю отрицательное число).

2. Реализовать функцию ```int length_of_lis(int* nums, int numsSize)```, которая  принимает целочисленный массив и его размер. И находит длину самой длинной непрерывной строго возрастающей подпоследовательности в данном массиве.

Пример: Для [10, 9, 2, 3, 7, 101, 18] LIS имеет длину 4 (например, [2, 3, 7, 101]).

1. Реализовать функцию ```int* merge(int* intervals, int intervalsSize, int* returnSize)```, которая  принимает целочисленный массив и количество интервалов. Входной массив сожержит интервалы чисел, например, ```{1,3,4,7}``` &mdash; интервалы ```[1; 3]```, ```[4; 7]```. Объедините все перекрывающиеся интервалы и верните массив непересекающихся интервалов, покрывающих все исходные интервалы в отсортированном виде. Через ```returnSize``` верните размер результирующего массива.

Пример: Вход: [[1,3],[2,6],[8,10],[15,18]]. Выход: [[1,6],[8,10],[15,18]].