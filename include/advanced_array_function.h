#ifndef ADVANCED_ARRAY_FUNCTION_H
#define ADVANCED_ARRAY_FUNCTION_H

#include <stdio.h>

int max_subarray_sum(int* nums, int size);
int length_of_lis(int* nums, int numsSize);
int* merge(int* intervals, int intervalsSize, int* returnSize);


#endif